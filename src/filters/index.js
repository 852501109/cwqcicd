// const filtersArr = require.context('./modules', true, /\.js$/)
// let filters = {}
// filtersArr.keys().forEach((item) => {
//   let filter = filtersArr(item).default
//   filters = Object.assign(filters, filter)
// })
// export default filters
